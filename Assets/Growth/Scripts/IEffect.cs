using UnityEngine;
using System.Collections;

public interface IEffect
{
    //public virtual void CalculateStrategy(Character character);
    //public virtual void CancelStrategy(Character character);
}
